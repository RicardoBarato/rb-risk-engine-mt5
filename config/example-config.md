# Example Configuration

Public configuration examples must be fictitious.

```text
account_number = 000000
broker_server = DEMO-SERVER
symbol = XAUUSD
risk_percent = educational-example-only
backtest_result = synthetic-example-only
```

Real configuration belongs in `config/mt5.local.ps1`, ignored local presets, or protected non-public storage. Do not commit real broker details, account identifiers or optimized parameters.
